import React from 'react';
import { assets } from '../../assets/assets';

export const navbar = () => {
    return (
        <div className='navbar'>
            <img src={assets.logo} />
        </div>
    );
};
